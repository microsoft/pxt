namespace control {
    /**
     * Schedules code that run in the background.
    //% help=functions/in-background shim=minecraft::runInBackground
     */
    export function inBackground(body: Action): void { }
}
