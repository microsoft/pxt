// Default and block
false && false

// Default or block
false || false

// Default not block
!(false)
!false

true
false

// Complex
false && (true || !false) || !(true && false)

// Expressions with variables
let x = 0
let y = false

y = x && ((((y)))) || false