# Continue

Skip the rest of the current loop and start the iteration again.


```block
while(true) {
    if (Math.randon() > 0.5) {
        // skip current iteration
        continue;
    }
    // do something
}
```

## #examples